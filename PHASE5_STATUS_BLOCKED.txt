PHASE 5 STATUS: SUPERSEDED BY PHASE 5.1
========================

Project: PS26227 - Semantic Retrieval and Multi-Temporal Change Analysis of Satellite Imagery
Phase: Phase 5 - Pixel-Level Cloud Masking and Quality Mask Preparation
Date: 2026-09-09

STATUS
------
This historical blocker is superseded by Phase 5.1. The s2cloudless route remains blocked for the configured L2A source because B10 is unavailable, but Phase 5.1 completed the real two-date quality-mask workflow using the provider-native Sentinel-2 SCL asset. See PHASE3_COMPLETE_OUTPUT.txt for the completion report.

WHAT WAS VALIDATED
------------------
- s2cloudless 1.7.0 installs on Python 3.11.
- libgomp1 is the only additional native runtime library required by the backend image.
- s2cloudless initializes with bundled model resources; no model download was required.
- CPU inference works on a small synthetic ten-band input.
- All Phase 2, Phase 3, Phase 4, and Phase 5 tests pass: 17 passed.
- Model input scaling is temporary only: stored Sentinel values are divided by 10000 for the model input and source rasters are not modified.

REQUIRED MODEL INPUT
--------------------
s2cloudless 1.7.0 requires these ten bands in order:
B01, B02, B04, B05, B08, B8A, B09, B10, B11, B12

The current Phase 4 L2A data has B02, B04, B08, and B11 prepared locally. The workflow can resolve the other L2A logical assets using the Earth Search mapping, but Earth Search's sentinel-2-l2a Item has no usable B10 asset.

REAL WORKFLOW RESULT
--------------------
Command:
docker run --rm --network host \
  -e STAC_API_URL=https://earth-search.aws.element84.com/v1 \
  -e STAC_COLLECTION=sentinel-2-l2a \
  -e SENTINEL_DATA_DIRECTORY=/app/data/raw/sentinel \
  -e DATA_DIRECTORY=/app/data \
  -e PROCESSED_DATA_DIRECTORY=/app/data/processed \
  -e OMP_NUM_THREADS=1 \
  -e OPENBLAS_NUM_THREADS=1 \
  -e MKL_NUM_THREADS=1 \
  -v "$PWD/data:/app/data" \
  sih_1-backend python /app/scripts/run_cloud_mask_workflow.py

Result:
Exit status 2 before cloud inference.

Error:
Configured STAC item cannot provide required s2cloudless bands: ['B10']. The current Earth Search L2A item has no usable B10 asset; do not substitute another band. Use a provider/product exposing valid B10 L1C data.

PROVIDER INVESTIGATION
----------------------
Matching sentinel-2-l1c Items were found, but the advertised Earth Search S3 B10 object URL returned HTTP 404. Mixing an L1C B10 value with L2A bands would be scientifically invalid, so it was not used.

OUTPUTS
-------
No cloud_probability.tif, cloud_mask.tif, quality_mask.tif, or quality_report.json was produced.
The Phase 4 prepared rasters remain unchanged.
PHASE3_COMPLETE_OUTPUT.txt remains the Phase 4 completion report and was not overwritten.
The temporary additional-band downloads and AOI cloud-input intermediates created during the first failed attempt were removed. The data directory is back to the Phase 4 state.

FILES IMPLEMENTED
------------------
- backend/processing/cloud/__init__.py
- backend/processing/cloud/input_builder.py
- backend/processing/cloud/s2cloudless_service.py
- backend/processing/cloud/quality_mask.py
- backend/processing/cloud/mask_validator.py
- scripts/run_cloud_mask_workflow.py
- tests/test_phase5.py

MODIFIED
--------
- backend/requirements.txt
- docker/Dockerfile.backend
- backend/settings.py
- .env.example
- configs/processing.yaml
- README.md

PHASE 5 TEST RESULT
-------------------
17 passed, including real s2cloudless inference on synthetic input.

Infrastructure regression also passed: Compose validation, backend image build, service checks, and frontend production build.

NEXT REQUIRED ACTION
--------------------
Configure a valid, accessible B10 source consistent with the cloud-model product. The clean options are:

1. Use a STAC provider exposing a valid Sentinel-2 L1C ten-band product and process all ten cloud-model bands from that product.
2. Configure a validated local B10 AOI asset with provenance.
3. Select a different cloud model whose documented input contract is satisfied by the existing L2A bands.

Do not substitute B11 for B10 and do not generate probabilities until one of these options is validated.

NEXT PHASE BOUNDARY
--------------------
Phase 5 remains open until the real two-date probability and quality-mask workflow succeeds. Phase 6 must not start yet.
